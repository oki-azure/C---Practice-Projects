#include <cstdio> // this is used to import the library cstdio(for input and output)

// main() is the function that runs when executed
int main(){  
    printf("Hello World!");
    // the printf function is responsible for output to the screen. It is from the cstdio library
    return 0;
    // the return 0 returns the 0 exit code to signal that the program should end, but it is optional
} 